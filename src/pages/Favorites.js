import React from "react";
import {SafeAreaView,View,Text} from "react-native";

export const Favorites = (props) => {
    return(
        <SafeAreaView>
            <View>
                <Text>Favorites</Text>
            </View>
        </SafeAreaView>
    )
}